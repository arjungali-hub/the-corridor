# Session log

## 2026-07-13

- Put existing prototype under git (baseline commit), added CLAUDE.md + this log.
- Completed the Phase 1 build order: **patching/bridging** (new-ink path between
  a torn group's end-nodes → paper patch stitched at both ends, rip stays,
  soft G4→D5 chime), **travel-history log** (feeds the ending later), and the
  fuller **test territory** (hunting loop Xi/Omicron, water route Pi/Rho with
  `creek` tear group + Sigma detour; Mu is now a shared junction between the
  highway detour and the water route).
- Design note: bridging counts only Aspen's new ink, never inherited ink — a
  patch is new knowledge, not the memory that just failed. A detour walked
  *before* its tear bridges instantly at tear time; that's intentional.
- Known grey-box quirks (parked): ink appears instantly rather than drawing
  progressively behind the mover; if the player is standing on an interior node
  when its group tears, the token floats inside the rip until they leave.
- Phase 1 remaining before Gate 1: the held-sense view doesn't apply cleanly to
  a map-only 2D prototype — decide whether to fake it (zoom/desaturate toggle)
  or defer to the full edition.

**Tomorrow's first action:** self-playtest a full session (tear all three
groups, bridge two), then decide the held-sense-view question above.
